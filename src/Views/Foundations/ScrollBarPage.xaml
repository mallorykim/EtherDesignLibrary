<Page
    x:Class="EtherSandbox.Views.Foundations.ScrollBarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:EtherSandbox.Views">

    <!--
        The global ScrollBar style in Controls/EtherScrollBar.xaml applies implicitly
        to every ScrollViewer in the app (see that file's header comment). INTERACTIVE
        shows the live bar against tall/wide content; STATES mirrors the Figma spec
        sheet's Default/Hover previews with static swatches drawn in the same two fills
        the live bar uses (so they cannot drift from the real thumb colours).
    -->
    <views:ComponentPage
        Title="Scroll Bar"
        Description="Updated 6 px scrollbar with a minimal thumb, no visible track, and no stepper arrows. The styling applies implicitly to every ScrollViewer in the sandbox.">

        <views:ComponentPage.InteractiveContent>
            <StackPanel Spacing="20">
                <StackPanel Spacing="8">
                    <TextBlock Text="Vertical (drag or hover the thumb)"
                               Style="{StaticResource micro/regular}"
                               Foreground="{ThemeResource TextTertiary}"/>
                    <Border Width="320" Height="240"
                            BorderBrush="{ThemeResource BorderSubtle}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource radius/control}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      AutomationProperties.Name="Vertical scroll viewer">
                            <StackPanel Padding="12" Spacing="12">
                                <TextBlock Text="Battery" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="Display" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="Storage" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="Network" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="Devices" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="Updates" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="Security" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="Performance" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="Accessibility" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="Support" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                                <TextBlock Text="About" FontFamily="{StaticResource InterFont}" FontSize="13" Foreground="{ThemeResource text/primary}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="8">
                    <TextBlock Text="Horizontal (drag or hover the thumb)"
                               Style="{StaticResource micro/regular}"
                               Foreground="{ThemeResource TextTertiary}"/>
                    <Border Width="420" Height="80"
                            BorderBrush="{ThemeResource BorderSubtle}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource radius/control}">
                        <ScrollViewer VerticalScrollBarVisibility="Disabled"
                                      HorizontalScrollBarVisibility="Auto"
                                      AutomationProperties.Name="Horizontal scroll viewer">
                            <StackPanel Orientation="Horizontal" Padding="12" Spacing="12">
                                <Border Width="80" Height="48" CornerRadius="6" Background="{ThemeResource BackgroundSurfaceRaised}"/>
                                <Border Width="80" Height="48" CornerRadius="6" Background="{ThemeResource BackgroundSurfaceRaised}"/>
                                <Border Width="80" Height="48" CornerRadius="6" Background="{ThemeResource BackgroundSurfaceRaised}"/>
                                <Border Width="80" Height="48" CornerRadius="6" Background="{ThemeResource BackgroundSurfaceRaised}"/>
                                <Border Width="80" Height="48" CornerRadius="6" Background="{ThemeResource BackgroundSurfaceRaised}"/>
                                <Border Width="80" Height="48" CornerRadius="6" Background="{ThemeResource BackgroundSurfaceRaised}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </StackPanel>
        </views:ComponentPage.InteractiveContent>

        <views:ComponentPage.StatesContent>
            <!-- Static swatches per the spec's STATE PREVIEWS: 8 x 150, radius 4, drawn
                 in the live bar's own two fills. -->
            <StackPanel Orientation="Horizontal" Spacing="80">
                <StackPanel Spacing="8" HorizontalAlignment="Center">
                    <TextBlock Text="Default"
                               Style="{StaticResource micro/regular}"
                               Foreground="{ThemeResource TextTertiary}"
                               HorizontalAlignment="Center"/>
                    <Border Width="6" Height="150"
                            CornerRadius="3"
                            Background="{ThemeResource EtherScrollBarThumbBrush}"/>
                </StackPanel>
                <StackPanel Spacing="8" HorizontalAlignment="Center">
                    <TextBlock Text="Hover"
                               Style="{StaticResource micro/regular}"
                               Foreground="{ThemeResource TextTertiary}"
                               HorizontalAlignment="Center"/>
                    <Border Width="6" Height="150"
                            CornerRadius="3"
                            Background="{ThemeResource EtherScrollBarThumbHoverBrush}"/>
                </StackPanel>
            </StackPanel>
        </views:ComponentPage.StatesContent>

    </views:ComponentPage>
</Page>
